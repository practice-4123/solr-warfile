name: Echo Script CI

on: [push]

jobs:
  echo_script_job:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Echo message
      run: echo "Hello, world!"
